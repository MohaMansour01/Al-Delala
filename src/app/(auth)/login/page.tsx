import LoginClient from "@/src/features/auth/components/LoginClient";

export default function LoginPage() {

  return (
    <section className="min-h-screen flex flex-col items-center justify-center">
      <LoginClient />
    </section>
  );
}
